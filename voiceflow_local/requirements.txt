# NirmiqEcho - Python Dependencies
# Install: pip install -r requirements.txt
# Versions pinned to the tested set (Python 3.12, Windows 11). Loosen to >= if
# you need newer builds.

sounddevice==0.5.5
numpy==2.4.2
scipy==1.16.3
webrtcvad-wheels        # pre-compiled VAD for Windows (Python 3.12+)
faster-whisper==1.2.1
noisereduce==3.0.3      # spectral noise reduction for noisy environments
python-dotenv==1.2.1    # .env config loading
pyautogui==0.9.54
keyboard==0.13.5
pyperclip==1.11.0
psutil==7.1.3           # RAM detection + battery info

# ── Jarvis Upgrade additions ─────────────────────────────────────────
pyttsx3==2.99           # TTS voice engine (Windows SAPI — zero download, fully offline)
send2trash==1.8.3       # Safe file deletion to Recycle Bin
PyYAML==6.0.3           # optional: load user-defined commands.yaml
pygetwindow==0.0.9      # Window focus / bring-to-front management

# System tray (optional — graceful fallback if not installed)
pystray==0.19.5
Pillow==12.0.0

# Mic auto-rescue: detect + undo Windows-level mic mute (optional)
pycaw==20251023
comtypes==1.4.16

# Optional GPU acceleration — NVIDIA CUDA only
# Uncomment the line below for 3-5x faster transcription:
# pip install torch --index-url https://download.pytorch.org/whl/cu121

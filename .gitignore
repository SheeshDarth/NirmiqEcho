# Secrets / local config
.env
.deps_ok

# Personal voice data — never publish the owner's recordings or speech profile
*.m4a
*.wav
voiceflow_local/assets/accent_profile.json
voiceflow_local/assets/command_log.txt
# Remembered facts may be sensitive (codes, passwords) — never commit
voiceflow_local/assets/memory.json
# A user's personal custom commands stay local (commands.example.yaml is the template)
commands.yaml

# Whisper models (several GB — re-downloaded / reused automatically)
models/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/

# Local data
*.db
.nirmiq-echo/

# Node / Electron (when the native shell lands)
node_modules/
ui/dist/
ui/.vite/

# Logs
*.log
logs/

# OS
Thumbs.db
desktop.ini

COMMIT_MSG.tmp

# NirmiqEcho - Python Dependencies
# Install: pip install -r requirements.txt

sounddevice>=0.4.6
numpy>=1.20
scipy>=1.7
webrtcvad-wheels        # pre-compiled VAD for Windows (Python 3.12+)
faster-whisper>=0.10.0
noisereduce>=3.0.0      # spectral noise reduction for noisy environments
python-dotenv>=1.0.0   # .env config loading
pyautogui>=0.9.54
keyboard>=0.13.5
pyperclip>=1.8.2
psutil>=5.9.0           # RAM detection + battery info

# ── Jarvis Upgrade additions ─────────────────────────────────────────
pyttsx3>=2.90           # TTS voice engine (Windows SAPI — zero download, fully offline)
send2trash>=1.8.0       # Safe file deletion to Recycle Bin
PyYAML>=6.0             # optional: load user-defined commands.yaml
pygetwindow>=0.0.9      # Window focus / bring-to-front management

# System tray (optional — graceful fallback if not installed)
pystray>=0.19.0
Pillow>=9.0.0

# Mic auto-rescue: detect + undo Windows-level mic mute (optional)
pycaw>=20240210
comtypes>=1.2.0

# Optional GPU acceleration — NVIDIA CUDA only
# Uncomment the line below for 3-5x faster transcription:
# pip install torch --index-url https://download.pytorch.org/whl/cu121
